{
  "name": "ethara-team-task-manager",
  "version": "1.0.0",
  "description": "Ethara.AI Full Stack Assessment — Team Task Manager",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && cross-env PORT=5001 npm start\"",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "postinstall": "npm run install:all",
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3"
  }
}
